### running application

Install npm dependencies
```sh
npm install
```

Install bower dependencies
```sh
bower install
```

Starting the server
```sh
node index.js
```

Then, go to `http://localhost:8083`
